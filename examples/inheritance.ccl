Vehicle := \ {
    self := {'vehicle': 1};
};

Car := \ {
    self := Vehicle();
    self.is_car = 1;
    self['number_of_wheels'] = 4;

    self.add_wheel = \ {
        self.number_of_wheels = self.number_of_wheels + 1;
    };

    self;
};

print(Car());
